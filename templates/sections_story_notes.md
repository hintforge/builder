# Sections -- Story Notes

**status:** scaffold <!-- scaffold | research-integrated | live-observed | reconciled -->
**deferred-to:** P1
**deferred-reason:** auto-aggregation target. Ingestion populates from every `vector: lore` claim in the corpus (grouped by chapter), OR promotes to `research-integrated` with the honest empty-statement template if the game has no story-beat content above tier 0. See [`../ingestion.md`](../ingestion.md) step 8.
**last_reconciled:** YYYY-MM-DD

Aggregated catalog of story beats, lore reveals, environmental storytelling, and narrative subtext -- the cross-cutting summary a player consults to answer "what does this section reveal about the world / story?"

Sourced from `vector: lore` tags scattered across per-zone / per-chapter / per-item files during research ingestion. Story-tier content lives behind the persona's spoiler dials; the persona reads this file only when the player's `enemy-tier` or `puzzle-tier` setting permits.

Until P1 ingestion runs, this file is empty by design -- its presence as a scaffold-with-deferral is the ingestion contract.
